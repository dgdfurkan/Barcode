/**
 * @deprecated — ayarlar js/db-config.js içinde. Geriye uyumluluk için bırakıldı.
 */
if (!window.JETBARKOD_VPS_API) {
    window.JETBARKOD_VPS_API = {
        enabled: true,
        baseUrl: 'https://api.flowcobalt.com',
    };
}
